// 20. Write a JavaScript program to find duplicate values in a JavaScript array.
const brojevi = [1, 2, 3, 3, 3, 4, 5, 6, 7, 7];

function nadjiDuplikate(arr) {
  const duplikati = [];
  for (let i = 0; i < arr.length; i++) {
    if (arr.includes(i)) {
    }
  }
}
